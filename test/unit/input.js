var expect = require('chai').expect;
var input = require('../..');

describe('cli-input:', function() {
  it('should capture message', function(done) {
    throw new Error('todo: implement tests');
    done();
  });
});
